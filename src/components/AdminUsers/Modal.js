import React from 'react';
import Modal from '../Modal';

export default function miModal(props) {
    return (
        <Modal myRef={props.myRef} title="Editar Usuarios">
            prueba de JCA de Usuarios
        </Modal>
    )
}