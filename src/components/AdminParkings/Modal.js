import React from 'react';
import Modal from '../Modal';

export default function miModal(props) {
    return (
        <Modal myRef={props.myRef} title="Editar Estacionamientos">
            prueba de JCA de Estacionamientos
        </Modal>
    )
}